import $ from 'jquery'

const domupdates = {
  changeLoadPage(userName, password, number) {
    if (userName === 'manager' && password === 'overlook2020') {
      console.log(1);
    } else if (userName === 'customer' && password === 'overlook2020') {
      console.log(2)
    }
  }



}
